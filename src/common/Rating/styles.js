import {
  StyleSheet
} from 'react-native';

const Styles = StyleSheet.create({
  wrapper: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignSelf: 'center'
  }
});

export default Styles;
