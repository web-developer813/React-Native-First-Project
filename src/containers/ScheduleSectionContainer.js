import React, { Component } from 'react';
import Schedule from '../components/Schedule';

const ScheduleSectionContainer = ({ ...props }) => (
  <Schedule {...props} />
);

export default ScheduleSectionContainer;
