import React, { Component } from 'react';
import ProviderListResult from '../components/ProviderListResult';

const ProviderListResultViewContainer = ({ ...props }) => (
  <ProviderListResult {...props} />
);

export default ProviderListResultViewContainer;
