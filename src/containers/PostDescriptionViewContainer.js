import React from 'react';
import PostDescriptionScreen from '../components/Post/PostDescriptionScreen';

const PostDescriptionViewContainer = ({ ...props }) => (
  <PostDescriptionScreen {...props} />
);

export default PostDescriptionViewContainer;
