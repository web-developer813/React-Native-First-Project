import React, { Component } from 'react';
import ScheduleViewScreen from '../components/Schedule/ScheduleViewScreen';

const ScheduleViewContainer = ({ ...props }) => (
  <ScheduleViewScreen {...props} />
);

export default ScheduleViewContainer;
