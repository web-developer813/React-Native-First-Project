import React, { Component } from 'react';
import ResetPassword from '../components/ResetPassword';

const ResetPasswordViewContainer = ({ ...props }) => (
  <ResetPassword {...props} />
);

export default ResetPasswordViewContainer;
