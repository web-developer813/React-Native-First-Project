import React, { Component } from 'react';
import ScheduleAddTime from '../components/Schedule/AddTime';

const ScheduleAddTimeContainer = ({ ...props }) => (
  <ScheduleAddTime {...props} />
);

export default ScheduleAddTimeContainer;
